---
description: Never download GGUF weights for tests; reuse installed local models only
alwaysApply: true
---

# Local GGUF testing (no agent downloads)

Maguna weights are multi‑GB. **Do not download models** (`huggingface-cli`, `hf download`, `wget`/`curl` to Hugging Face, etc.) unless the user **explicitly** asks you to fetch a file.

## Before any spike / inference smoke test

1. **List what is already on disk** (run in repo root):

```bash
find src-tauri/target/debug/Models src-tauri/target/release/Models -name '*.gguf' 2>/dev/null
```

During dev, installs usually land under `src-tauri/target/debug/Models/<model_id>/<model_id>.gguf` with a `manifest.json`. Release builds use `src-tauri/target/release/Models/` the same way.

2. **Use an existing GGUF** — pick the smallest installed model that matches the template you need to verify.

3. **If nothing is installed**, tell the user which catalog model to install in the app (or ask them to confirm a path). **Do not download it yourself.**

4. **Leave `.spike-models/` empty.** It is gitignored scratch space; do not populate it with new downloads.

## spike_load

Instruct models need a **family-specific chat template** in the prompt (see `src-tauri/src/chat_template.rs`). Raw user text is only a rough smoke test.

```bash
cd src-tauri && cargo run --example spike_load --features llama -- \
  "/absolute/path/to/model.gguf" "<formatted prompt>"
```

Prefer the user's installed path from step 1 over any cached copy elsewhere.

## Cleanup

If you ever downloaded weights for an experiment, **delete those files** when done. Do not leave multi‑GB duplicates beside the user's `Models/` install.
